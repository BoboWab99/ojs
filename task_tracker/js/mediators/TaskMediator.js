/* 
Between backend and frontend.
*/

// Create Task
// Update Task
// Delete Task

class TaskMediator extends OpenScript.Mediator {
    /**
     * Create a Task
     * @param {Task} task 
     */
    create(task) {

    }

    /**
     * Update a Task
     * @param {Task} task 
     */
    update(task) {}

    /**
     * Delete a Task
     * @param {Task} task 
     */
    delete(task) {}
}